# win-lite / win-full 开发记录

## 第一阶段：资源校验

本阶段只验证输入资源是否可用于后续 win-lite / win-full 打包方案，没有修改现有业务逻辑。已读取 `build/win-lite/manifest.json`，确认版本为 `b9244`，下载基地址为 `https://rongxinai.krli.org/llamacpp/b9244`，并包含 `win-arm64`、`win-x64`、`win-x64-cuda-12` 三个 Windows backend。随后检查 `build/win-full` 目录，确认三个 zip 文件均存在，文件名与 manifest 中的 `archive.assetName` 一致。进一步校验 zip 内容后，三个压缩包都能找到 `llama-server.exe`，说明基础解压安装路径具备可行性。

校验结果中，`llama-b9244-bin-win-cpu-arm64.zip` 和 `llama-b9244-bin-win-cpu-x64.zip` 的 SHA256 与 manifest 完全一致；但 `llama-b9244-bin-win-cuda-12.4-x64.zip` 的实际 SHA256 为 `5dd3114efea49947ccd2cfc371463555f8144c5d72aef301421b6ca9d8244e36`，manifest 中记录的是 `ccce2e334df5679755b17228025ea475551e374a4a135e7925aaea6420db106a`，二者不一致。建议先确认 CUDA zip 是否重新打包过，或更新 manifest 后再进入第二阶段。
## 第一阶段复核：SHA256 重新检查

收到资源更新后，重新以 `build/win-lite/manifest.json` 为准校验 `build/win-full` 下三个 zip 文件。`win-arm64` 与 `win-x64` 仍然通过校验，文件存在、SHA256 与 manifest 一致，并且压缩包内均可找到 `llama-server.exe`。`win-x64-cuda-12` 的 zip 文件也存在，且内部可以找到 `llama-b9244-bin-win-cuda-12.4-x64/llama-server.exe`，说明压缩包结构本身可用于后续解压安装验证。

但 CUDA 包的 SHA256 仍未与 manifest 匹配。本次实际计算值为 `ccce2e334df5679755b17228025ea475551e374a4a135e7925aaea6420db106a`，manifest 中记录仍为 `c51dbbc1c55fdb3a1bcf25eefcdac4e4829e5d165438a2f2516bb78455c4483a`。与上次相比，CUDA zip 文件大小和哈希都发生变化，说明文件已更新，但 manifest 没有同步到当前文件版本。建议将 manifest 中 CUDA backend 的 `archive.sha256` 更新为当前实际值，或再次替换为与 manifest 匹配的 zip 后再进入第二阶段。
## 第一阶段复核：资源已通过

根据更新后的 `build/win-lite/manifest.json` 重新校验资源，三份 `build/win-full` 下的 zip 均已通过 SHA256 校验，并且压缩包内都能找到 `llama-server.exe`。其中 `win-arm64` 对应 `llama-b9244-bin-win-cpu-arm64.zip`，`win-x64` 对应 `llama-b9244-bin-win-cpu-x64.zip`，`win-x64-cuda-12` 对应 `llama-b9244-bin-win-cuda-12.4-x64.zip`。CUDA 包当前 SHA256 为 `ccce2e334df5679755b17228025ea475551e374a4a135e7925aaea6420db106a`，已经与 manifest 保持一致。第一阶段资源输入现在满足进入第二阶段的条件。

## 第二阶段：安装选择逻辑验证

本阶段只基于 manifest 数据模拟 Windows runtime 选择逻辑，没有接入真实安装器，也没有修改现有代码。验证规则为：若系统为 Windows ARM64，则直接选择 `win-arm64`，不检测 GPU；若系统为 Windows x64，则继续判断是否存在 NVIDIA GPU/CUDA，可用时默认选择 `win-x64-cuda-12`，不可用时选择 `win-x64` CPU 版本。模拟结果显示，`win32/arm64` 无论是否传入 GPU 标记都会选择 `win-arm64`；`win32/x64` 在无 GPU 时选择 `win-x64`，有 GPU 时选择 `win-x64-cuda-12`。边界情况中，`win32/ia32` 和非 Windows 平台均标记为不支持。当前 manifest 的三个 backend 可以完整覆盖目标 Windows 场景。

## 第三阶段：win-full 本地解压验证

本阶段在项目 `tmp` 目录下模拟 win-full 安装，不写入真实用户目录。三个 zip 均可解压并定位到 `llama-server.exe`，整理后的 backend 结构为 `backends/b9244/win-arm64`、`win-x64`、`win-x64-cuda-12`，每个目录下均包含 `bin/llama-server.exe` 与 `runtime-build-info.json`，能够满足现有程序按 `current/bin/llama-server.exe` 查找的基础条件。验证中发现，若用目录复制方式反复覆盖 `current`，Windows 下可能出现删除失败或残留旧文件，因此后续实现应沿用现有 `syncCurrentBackend` 的 junction 指向机制，而不是直接覆盖 `current`。由于当前终端缺少 `node` 命令，junction 切换仍需在完整 Node 环境中补充验证。

## 第四阶段：win-lite 下载链路评估

本阶段验证 win-lite 的 manifest 下载链路及现有代码可复用范围。当前 `build/win-lite/manifest.json` 可按 `releaseBaseUrl + "/" + archive.assetName` 拼出三个下载地址，且文件名与 `build/win-full` 内置 zip 完全一致，说明 win-lite 与 win-full 可以共享同一套 backend 元数据。现有 `llamacppBackendManager` 已支持 manifest、zip 解压、SHA256 校验、安装到 `backends/<version>/<backend>`、以及通过 `current` junction 切换 backend；渲染层也已有 `listBackends`、`installBackend` 和安装进度条事件。主要缺口是项目尚未安装 `node-downloader-helper`，现有下载函数使用 `fetch`，不满足指定库、断点续传和 3 次重试要求。下一阶段建议验证依赖安装与下载器封装方案。

## 续接快照：明日复现入口

当前讨论目标是将 Windows llama.cpp 打包拆成 `win-lite` 与 `win-full` 两种形式，并先验证方案有效性，暂不修改业务逻辑。已完成四个阶段：资源 SHA256 与 zip 内容校验通过；Windows 架构/GPU 选择规则通过；win-full 三个 zip 可整理为统一 backend runtime 结构；win-lite manifest URL 拼接与现有 backend 安装框架匹配。关键资源为 `build/win-lite/manifest.json` 和 `build/win-full/*.zip`，版本固定 `b9244`。下一阶段应验证 `node-downloader-helper` 依赖安装与封装方案，使下载支持断点续传和 3 次重试。注意事项：`current` 不应使用复制覆盖方式切换，应沿用 junction；当前终端缺少 `node` 与 `git`，完整验证需在具备 Node.js 的环境执行。

## 第五阶段：node-downloader-helper 接入预检

本阶段检查 `node-downloader-helper` 是否已进入项目，并评估最小接入点。当前项目根目录的 `package.json`、`package-lock.json`、`node_modules`、用户下载目录与 npm 缓存中均未发现该依赖或离线包，终端也无法调用 `node`、`npm`、`git`，因此暂时不能验证库的真实 API、断点续传行为和 3 次重试配置。代码层面已确认最小改动点在 `src/main/libs/llamacppBackendManager.ts` 的内部 `downloadFile()`：上层 `downloadArchive()` 已处理 manifest URL、进度回调、SHA256 校验和 zip 解压，renderer 也已能显示 `completed/total/speed/percent` 进度。下一步需要先让依赖真实出现在项目中，再将该下载函数替换为基于 `DownloaderHelper` 的封装。

## 第五阶段补充：下载器能力验证

已使用 `C:\Downloads\edge\node-v24.18.0-x64.msi` 安装 Node.js，当前可通过 `C:\Program Files\nodejs\node.exe` 与 `npm.cmd` 调用。用户提供的 `C:\WINDOWS\system32\node_modules\node-downloader-helper` 存在，版本为 `2.1.11`，类型定义包含 `retry: { maxRetries, delay }`、`resumeIfFileExists`、`resumeOnIncomplete`、`progress.throttled` 等能力。使用本地 HTTP 服务完成最小验证：先制造 256KB 半截文件，再触发一次 HTTP 500，库按 `retry` 进行重试，并通过 `Range: bytes=262144-` 续传，最终文件大小与内容均匹配原始数据。项目依赖安装暂未成功，因为 npm 环境变量 `offline=true` 导致 registry 与本地路径安装都触发未缓存依赖失败；编码前需解决依赖写入 `package.json/package-lock.json` 的方式。

## 续接快照：2026-07-03 当前节点

当前应继续围绕 Windows llama.cpp `win-lite` / `win-full` 打包方案推进。已验证：b9244 三个 zip 资源、manifest URL 规则、架构/GPU 选择、win-full 解压结构、现有 backend 安装框架，以及 `node-downloader-helper` 的断点续传和 3 次重试能力。Node.js 已安装在 `C:\Program Files\nodejs`，但当前会话可能仍需使用绝对路径调用。`node-downloader-helper` 目前只存在于 `C:\WINDOWS\system32\node_modules`，尚未写入项目依赖；`npm install` 因环境变量 `offline=true` 失败，且 `package.json/package-lock.json/node_modules` 未包含该依赖。下一阶段入口：先解决依赖入库，再替换 `src/main/libs/llamacppBackendManager.ts` 内部 `downloadFile()`。

## 第六阶段：下载器依赖入库与接入完成

重启 VS Code 服务后，`node` 与 `npm` 已能从 PATH 正常调用，版本分别为 Node.js `v24.18.0` 与 npm `11.16.0`。普通沙箱内 registry 请求仍会被拒绝，但提权网络环境下 `npm view node-downloader-helper version --offline=false --prefer-online` 成功返回 `2.1.11`。已将 `node-downloader-helper@^2.1.11` 写入 `package.json` 与 `package-lock.json`，并确认项目 `node_modules` 可解析 `node-downloader-helper@2.1.11`，`require('node-downloader-helper')` 能正常取得 `DownloaderHelper` 导出。注意：`npm config get offline` 仍返回 `true`，后续需要在线访问 registry 的 npm 命令建议继续显式带上 `--offline=false --prefer-online`，或手动调整 npm 配置。

代码层面已替换 `src/main/libs/llamacppBackendManager.ts` 内部 `downloadFile()`：保留原有 URL fallback 逻辑与上层进度回调契约，具体下载改为 `DownloaderHelper`，开启 `resumeIfFileExists`、`resumeOnIncomplete`、`resumeOnIncompleteMaxRetry: 3`、`retry: { maxRetries: 3, delay: 1000 }`，并继续上报 `completed/total/speed`。实现会清理旧版 `.download` 残留文件，实际下载直接写入目标归档路径，以便库基于已有半截文件发起 `Range` 续传。

测试层面更新了 `src/main/libs/llamacppBackendManager.test.ts`：新增本地 HTTP 归档服务，覆盖真实下载进度、一次 HTTP 500 后重试、以及预置半截 zip 后的 `Range: bytes=<offset>-` 续传；同时 mock 导入后的设备探测，避免空测试二进制触发真实 `llama-server --list-devices`。验证结果：`npx vitest run src/main/libs/llamacppBackendManager.test.ts` 通过，`npm test -- llamacppBackendManager` 通过，`npm run compile:electron` 通过，针对改动文件的 `npm run lint -- src/main/libs/llamacppBackendManager.ts src/main/libs/llamacppBackendManager.test.ts` 通过。下一阶段可以开始把 win-lite / win-full 的打包脚本或安装分发入口与该 backend 下载能力串起来。

## 第七阶段：win-lite / win-full 打包入口接入

本阶段已把 Windows llama.cpp backend 资源接入打包和运行时安装闭环。新增 `src/main/libs/llamacppBackendResources.ts`，仅在 Windows 平台启用：打包模式下读取 `process.resourcesPath/llamacpp-backends/manifest.json`，开发模式下读取仓库 `build/win-lite/manifest.json`，并在 `build/win-full` 或随包资源中存在对应 zip 时把 archive URL 注入为 `file://`。`fetchLlamaCppBackendManifest()` 现在在未显式设置 `LLAMACPP_BACKEND_MANIFEST_URL` 时优先使用该内置 manifest；显式环境变量仍保留最高优先级。`downloadFile()` 已支持 `file://` 本地归档，采用流式复制并保留 `completed/total/speed` 进度回调，然后继续走既有 SHA256 校验、zip 解压、安装到 `backends/<version>/<backend>` 和 `current` junction 切换逻辑。

打包侧已修改 `scripts/electron-builder-hooks.cjs`：Windows `win-resources.tar` 会加入 `llamacpp-backends` 资源。通过环境变量 `RONGXINAI_WIN_LLAMACPP_BACKEND_BUNDLE` 控制模式，取值为 `lite`、`full` 或 `none`。`lite` 只打入 manifest，安装时按 manifest 远程下载；`full` 打入 manifest 以及 manifest 中引用的三份 `build/win-full` zip，安装时直接走本地 `file://` zip。staging 目录优先使用稳定路径以保留 tar cache 可复用性；若 Windows 删除 staging 时偶发 EPERM，则自动使用唯一临时目录兜底，tar 内最终 prefix 仍固定为 `llamacpp-backends`。`package.json` 中 `dist:win` 现在指向 `dist:win:lite`，并新增显式脚本 `dist:win:lite` 与 `dist:win:full`。

验证结果：手动调用 `prepareWindowsLlamaCppBackendResources('lite')` 得到仅 `manifest.json`，调用 `prepareWindowsLlamaCppBackendResources('full')` 得到 `manifest.json` 加三份 zip：`llama-b9244-bin-win-cpu-arm64.zip`、`llama-b9244-bin-win-cpu-x64.zip`、`llama-b9244-bin-win-cuda-12.4-x64.zip`。新增/更新测试覆盖内置 manifest 注入本地 archive URL、`file://` 归档安装、本地 HTTP 下载重试和 Range 续传；`npx vitest run src/main/libs/llamacppBackendManager.test.ts` 通过，`npm test -- llamacppBackendManager` 通过，`npm run compile:electron` 通过，`npm run lint -- src/main/libs/llamacppBackendManager.ts src/main/libs/llamacppBackendManager.test.ts src/main/libs/llamacppBackendResources.ts` 通过。下一阶段建议执行一次实际 `dist:win:lite` 和 `dist:win:full` 打包冒烟，检查安装包体积、安装后 `resources/llamacpp-backends` 内容，以及首次安装 backend 时是否分别走远程下载和本地 zip。

• 打包命令：

  npm run dist:win:lite

  npm run dist:win:full





## 需求变更：llama.cpp backend 改为 NSIS 安装阶段完成

本次需求调整为：`win-lite` 与 `win-full` 两种 Windows 安装包都必须在 NSIS 安装阶段完成 llama.cpp backend 安装。安装完成后，App 首次启动时不再进入“安装 backend / 选择 backend”的流程，而是直接通过现有 `llamacpp-runtime/current` 结构检测到已安装的 `llama-server.exe`，后续只负责启动和管理进程。

目标安装目录继续沿用现有运行时布局，避免改动 App 主流程：

```text
%APPDATA%\RongxinAI\llamacpp-runtime\
  backends\
    <version>\
      <backend>\
        build\
          bin\
            llama-server.exe
            *.dll
        runtime-build-info.json
  current -> backends\<version>\<backend>
```

推荐 backend 在安装阶段自动选择，启动阶段不再让用户选择。当前规则为：Windows x64 且检测到 NVIDIA GPU 时选择 `win-x64-cuda-12`；Windows x64 未检测到 NVIDIA GPU 时选择 `win-x64`；Windows ARM64 选择 `win-arm64`。后续如 manifest 扩展 CUDA 13、Vulkan、HIP 等 backend，再按同一选择函数扩展优先级。

`win-full` 安装包继续携带 `manifest.json` 与 backend zip。NSIS 解压 `win-resources.tar` 后，安装 helper 从 `$INSTDIR\resources\llamacpp-backends` 读取 manifest 与本地 zip，离线完成解压、SHA256 校验、写入 `runtime-build-info.json` 与 `current` junction 切换，不依赖网络。

`win-lite` 安装包只携带 `manifest.json`，不携带 CPU 或 CUDA backend zip。NSIS 解压资源后，安装 helper 只下载当前机器推荐的一个 backend，不下载全部 backend。下载必须使用 `node-downloader-helper`，开启默认自动断点续传与重试；用户不需要手动选择 Retry/Cancel。若断点续传和重试全部失败，NSIS 必须中止安装，并提示用户检查网络、代理或防火墙配置后重新运行安装器。

建议新增一个 NSIS 专用 helper，例如 `scripts/install-llamacpp-backend-nsis.cjs`，并在 Windows 打包时以可被安装器调用的形式放入资源目录。该 helper 负责读取 manifest、选择 backend、判断本地 zip 或远程 URL、下载/复制归档、校验 SHA256、解压、整理目录、写入 build info、创建 `current` junction，并将诊断日志写入 `%APPDATA%\RongxinAI\install-llamacpp.log`。NSIS 中应在 `win-resources.tar` 解压完成后立即调用该 helper；helper 返回非 0 时，安装器弹出失败提示并 `Abort`。

## 后续执行阶段与审核点

以下阶段按顺序执行。每个阶段完成后先停下来汇报结果，等待审核确认后再进入下一阶段。

### 阶段 1：实现前基线清理与方案冻结

目标：确认当前仓库可进入实现状态。重点检查并处理会阻断编译/打包的 Git 冲突标记，尤其是 `package.json`、`scripts/electron-builder-hooks.cjs`、`src/main/libs/llamacppBackendManager.ts`、`src/main/libs/llamacppManager.ts`、`src/main/ipcHandlers/llamacpp.ts`、`src/shared/llamacpp/constants.ts`、`src/renderer/types/electron.d.ts` 等 llama.cpp 相关文件。

交付物：冲突处理清单、保留分支逻辑说明、编译前风险列表。

审核点：确认冲突解决方向正确，且没有提前改变业务行为。

阶段 1 执行结果：已完成实现前基线清理。仓库中阻断解析/编译的 Git 冲突标记已清除，覆盖范围包括 `package.json`、`package-lock.json`、`scripts/electron-builder-hooks.cjs`、`scripts/build-openclaw-runtime.sh`、`src/main/libs/llamacppBackendManager.ts`、`src/main/libs/llamacppManager.ts`、`src/main/ipcHandlers/llamacpp.ts`、`src/shared/llamacpp/constants.ts`、`src/renderer/types/electron.d.ts`、本地推理页面和相关测试文件等。保留方向为：以当前已拆分、已清理的 `47d6cc54` 基线为主，避免恢复旧的大文件 UI、旧的 llama.cpp 服务配置 IPC、以及旧的 OpenClaw 模型绑定入口；同时保留既有 win-lite / win-full 打包与下载链路改动，包括 `dist:win:lite` / `dist:win:full`、`node-downloader-helper@^2.1.11`、Windows `llamacpp-backends` 资源打包函数、内置 manifest 读取、`file://` 本地归档支持，以及基于 `DownloaderHelper` 的断点续传/自动重试下载封装。

本阶段没有新增 NSIS helper，也没有把 llama.cpp backend 安装迁移到 NSIS 阶段；业务流程仍停留在已有 App 运行时可安装/切换 backend 的能力。最小验证结果：`package.json` 与 `package-lock.json` 可被 Node 解析，`node-downloader-helper` 可解析到 `DownloaderHelper`，`scripts/electron-builder-hooks.cjs` 通过 `node --check`，Windows bundle mode resolver 可返回 `lite` / `full`，全仓库已无 `<<<<<<<` / `=======` / `>>>>>>>` 冲突标记。`npm run compile:electron` 已通过；普通沙箱内因 `app-builder.exe` 执行权限出现 `spawn EPERM`，提权后通过。`npx vitest run src/main/libs/llamacppBackendManager.test.ts` 已通过，普通沙箱内同样因 Vite realpath 子进程 `spawn EPERM`，提权后 15 个测试通过。

编译前风险列表：`win-lite` / `win-full` 目前仍只是打包资源入口，尚未接入 NSIS 安装阶段；安装失败时的 NSIS 中止与网络/代理/防火墙提示仍需后续阶段实现；当前 App 启动侧仍保留高级 backend 安装/切换 IPC 和页面能力，是否隐藏或调整需在阶段 6 收口确认；阶段 1 中未执行完整 Windows 安装包构建，只完成编译、脚本语法和 llama.cpp backend 单测验证。

### 阶段 2：NSIS helper 设计与最小实现

目标：新增安装阶段专用 helper，先实现 manifest 读取、平台/架构/GPU 推荐 backend 选择、安装目录计算、日志输出和退出码约定。此阶段不接入 NSIS，也不真实下载大包。

交付物：helper 入口、选择逻辑测试、失败退出码和日志格式说明。

审核点：确认 backend 选择规则符合产品预期，失败时能给 NSIS 明确错误。

阶段 2 执行结果：已新增 NSIS 安装阶段专用 helper 最小入口 `scripts/install-llamacpp-backend-nsis.cjs`。当前 helper 只实现 dry-run 安装计划，不执行下载、解压、SHA256 校验、写入 runtime 或创建 `current` junction，因此不会改变现有业务行为。入口支持读取 `manifest.json`、解析资源目录、计算 `%APPDATA%\RongxinAI\llamacpp-runtime`、计算 `backends/<version>/<backend>`、`current`、`downloads` 等目标路径，并输出 JSON 安装计划。日志默认写入 `%APPDATA%\RongxinAI\install-llamacpp.log`，也可通过 `--app-data-dir` / `--log-path` 覆盖，便于后续 NSIS 调用时定位失败原因。

推荐 backend 选择规则已固化为：`win32/arm64` 选择 `win-arm64` CPU；`win32/x64` 且检测到 NVIDIA GPU 时优先选择 CUDA 12 backend，即当前 manifest 中的 `win-x64-cuda-12`；`win32/x64` 未检测到 NVIDIA GPU 或 manifest 中没有 CUDA backend 时选择 `win-x64` CPU；非 Windows 或不支持架构返回非 0 计划。当前阶段支持通过 `--platform`、`--arch`、`--has-nvidia-gpu` 覆盖探测值，方便测试；真实运行时会用 `process.platform`、`process.arch`，并在 Windows x64 上用 `nvidia-smi -L` 做最小 NVIDIA 检测。

退出码约定为：`0` 成功生成安装计划；`2` 参数错误；`10` manifest 读取或 JSON 解析失败；`11` 平台/架构不支持；`12` manifest 中没有兼容 backend；`70` 预留给后续真实安装阶段的未预期失败。win-lite 场景下若资源目录只有 manifest，计划中的 archive source 为 `remote`，并根据 `releaseBaseUrl + "/" + assetName` 生成下载 URL；win-full 场景下后续资源目录包含 zip 时，计划会识别为 `local` 并生成 `file://` 本地归档 URL。

验证结果：`node --check scripts/install-llamacpp-backend-nsis.cjs` 通过；使用 `build/win-lite/manifest.json` 执行 dry-run，可生成 `b9244/win-x64` 的远程下载计划；新增 `tests/install-llamacpp-backend-nsis.test.ts` 覆盖 Windows ARM64、Windows x64 CPU、Windows x64 NVIDIA CUDA、CUDA 缺失回退 CPU、非 Windows 不支持，以及 dry-run 计划和不支持架构退出码，共 7 个测试，`npx vitest run tests/install-llamacpp-backend-nsis.test.ts` 已通过。普通沙箱内 Vitest 仍会遇到 Windows realpath 子进程 `spawn EPERM`，提权后通过。

### 阶段 3：win-full 本地安装链路

目标：让 helper 支持从 `$INSTDIR\resources\llamacpp-backends` 使用本地 zip 安装推荐 backend。实现 SHA256 校验、zip 解压、复制到 `%APPDATA%\RongxinAI\llamacpp-runtime\backends\<version>\<backend>`、写入 `runtime-build-info.json`、创建 `current` junction。

交付物：本地 zip 安装测试、目录结构截图/日志、重复安装覆盖策略说明。

审核点：确认 `win-full` 可完全离线完成 llama.cpp backend 安装。

阶段 3 执行结果：已将 `scripts/install-llamacpp-backend-nsis.cjs` 从 dry-run 扩展为 win-full 本地 zip 真实安装链路。helper 在非 `--dry-run` 模式下只接受本地 archive；如果安装计划中的 archive source 不是 `local`，会以 `13` 退出并提示 win-full 安装需要本地归档，因此本阶段不会触发 win-lite 远程下载。安装流程为：读取 manifest 并选择推荐 backend，确认资源目录中存在对应 zip，校验 SHA256，解压到临时 staging，定位 `llama-server.exe`，将可执行文件所在目录内容复制到 `llamacpp-runtime/backends/<version>/<backend>/build/bin`，写入 `runtime-build-info.json`，再把 `current` 切换为指向该 backend 目录的 junction。

重复安装覆盖策略：先在 `.install-*` 临时目录中完成解压、复制和 build info 写入，校验 staged backend 中存在 `build/bin/llama-server.exe` 后，才替换目标 backend 目录。目标目录替换优先使用 rename；在 Windows 上如果跨目录/安全策略导致 rename 出现 `EPERM`，会自动回退为目录复制。`current` 先删除旧 junction 或旧目录，再创建新的 junction。临时目录清理采用 best-effort；如果 Windows 文件句柄短暂占用导致清理 `EPERM`，只写日志，不影响已经完成的安装结果。

验证结果：新增测试覆盖小型本地 zip 安装，确认 `build/bin/llama-server.exe`、`runtime-build-info.json` 和 `current` junction 均生成成功；`npx vitest run tests/install-llamacpp-backend-nsis.test.ts` 通过，共 8 个测试。额外使用真实 `build/win-full/llama-b9244-bin-win-cpu-x64.zip` 做项目 `tmp` 目录冒烟安装，选择 `b9244/win-x64`，安装后 `tmp/.../llamacpp-runtime/current/build/bin/llama-server.exe` 存在，`runtime-build-info.json` 中记录 source 为 `nsis-win-full`，archive 为 `llama-b9244-bin-win-cpu-x64.zip`，SHA256 为 `e60c6f089316dfa38e3c6f612ae42d39cde240777e129ab865229ef5796ee1bf`。本阶段仍未接入 NSIS，也未实现 win-lite 远程下载链路。

### 阶段 4：win-lite 远程下载链路

目标：让 helper 在本地 zip 不存在时按 manifest URL 下载推荐 backend。下载使用 `node-downloader-helper`，启用断点续传、自动重试、失败后非 0 退出。下载缓存建议放在 `%APPDATA%\RongxinAI\llamacpp-runtime\downloads`，以便安装器重新运行时续传。

交付物：本地 HTTP 模拟测试，覆盖正常下载、HTTP 失败重试、半截文件 Range 续传、最终失败退出。

审核点：确认 `win-lite` 只下载一个推荐 backend，且失败策略为自动重试后中止安装。

阶段 4 执行结果：已将 `scripts/install-llamacpp-backend-nsis.cjs` 扩展为 win-lite 远程下载真实安装链路。安装计划仍只根据当前机器平台、架构和 NVIDIA GPU 探测结果选择一个推荐 backend；当 `$INSTDIR\resources\llamacpp-backends` 中不存在对应 zip 时，helper 会按 manifest 中的 `archive.url` 或 `releaseBaseUrl + "/" + archive.assetName` 下载该单个归档，不会下载其它 CPU/CUDA backend。下载缓存位置为 `%APPDATA%\RongxinAI\llamacpp-runtime\downloads\<assetName>`，安装器重新运行时可基于已存在的半截文件继续续传。

下载实现已接入 `node-downloader-helper`，配置为 `resumeIfFileExists: true`、`resumeOnIncomplete: true`、`resumeOnIncompleteMaxRetry: 3`、`retry: { maxRetries: 3, delay: 1000 }`、`removeOnFail: false`、`removeOnStop: false`。为避免连续 HTTP 5xx 或网络异常时第三方库在 HEAD/GET 与续传重试之间长时间循环，helper 额外记录合并重试事件并在自动重试耗尽后主动停止下载，统一以退出码 `16` 返回 `Download failed after automatic retries. Please check network, proxy, or firewall settings.`。下载成功后仍复用阶段 3 的安装流程：SHA256 校验、解压到临时 staging、复制到 `backends/<version>/<backend>/build/bin`、写入 `runtime-build-info.json`、切换 `current` junction。远程安装写入的 build info source 为 `nsis-win-lite`，本地 win-full 仍为 `nsis-win-full`。

验证结果：`node --check scripts/install-llamacpp-backend-nsis.cjs` 通过；`tests/install-llamacpp-backend-nsis.test.ts` 新增本地 HTTP 归档服务，覆盖正常远程下载并安装、一次 HTTP 500 后自动重试、预置半截 zip 后使用 `Range: bytes=<offset>-` 续传、以及持续 HTTP 500 后返回退出码 `16` 并提示检查网络/代理/防火墙。`npx vitest run tests/install-llamacpp-backend-nsis.test.ts` 已通过，共 11 个测试。本阶段仍未接入 NSIS 脚本，也未改变 App 启动侧行为。

### 阶段 5：打包资源与 NSIS 接入

目标：将 helper 放入 Windows 安装资源，并在 `scripts/nsis-installer.nsh` 的 `win-resources.tar` 解压完成后调用。`win-full` 继续打入 manifest + zip；`win-lite` 只打入 manifest + helper。NSIS 捕获 helper 退出码，失败时弹出“请检查网络、代理或防火墙配置”的提示并中止安装。

交付物：`dist:win:lite` 与 `dist:win:full` 资源内容清单、NSIS 调用日志、失败提示文案。

审核点：确认安装阶段调用时机正确，不影响 Python、uv、OpenClaw、SKILLs 等已有资源安装。

阶段 5 执行结果：已完成打包资源与 NSIS 安装阶段接入。`scripts/electron-builder-hooks.cjs` 现在会在 Windows `win-resources.tar` 中额外加入 `llamacpp-nsis-helper/` 目录；当 bundle mode 为 `lite` 或 `full` 时都会包含 `install-llamacpp-backend-nsis.cjs`，并把该 helper 运行所需的最小 Node 依赖闭包复制到 `llamacpp-nsis-helper/node_modules`。依赖闭包当前包含 `node-downloader-helper`、`extract-zip` 以及 `yauzl`、`fd-slicer`、`pump` 等递归依赖，避免安装阶段依赖 app.asar 内的包解析。`win-lite` 的 `llamacpp-backends/` 仍只包含 `manifest.json`；`win-full` 的 `llamacpp-backends/` 包含 `manifest.json` 与三份 backend zip。

`scripts/nsis-installer.nsh` 已在 `win-resources.tar` 解压完成、Python/uv 运行时校验通过之后调用 llama.cpp backend helper。调用方式为通过已安装的 `$INSTDIR\RongxinAI.exe` 设置 `ELECTRON_RUN_AS_NODE=1` 执行 `$INSTDIR\resources\llamacpp-nsis-helper\install-llamacpp-backend-nsis.cjs`，并显式传入 `--manifest "$INSTDIR\resources\llamacpp-backends\manifest.json"`、`--resources-dir "$INSTDIR\resources\llamacpp-backends"`、`--app-data-dir "$APPDATA\RongxinAI"`、`--log-path "$APPDATA\RongxinAI\install-llamacpp.log"`。helper 返回非 0 时 NSIS 会中止安装；其中退出码 `16` 会显示网络/代理/防火墙检查提示，其它非 0 退出会提示查看 `install-llamacpp.log` 与 `install-timing.log`。如果 bundle mode 显式为 `none` 且 manifest/helper 都不存在，NSIS 会记录 skip 并继续，以保留调试模式。

验证结果：`node --check scripts/electron-builder-hooks.cjs`、`node --check scripts/install-llamacpp-backend-nsis.cjs` 均通过；新增 `tests/electron-builder-hooks.test.ts` 覆盖 bundle mode 解析、win-lite 不携带 zip、以及 NSIS helper 依赖闭包 staging；连同阶段 4 helper 测试一起运行 `npx vitest run tests/electron-builder-hooks.test.ts tests/install-llamacpp-backend-nsis.test.ts` 已通过，共 14 个测试。手动 staging 冒烟结果：`lite` 资源为 `llamacpp-backends/manifest.json`；`full` 资源为 `manifest.json` 加三份 zip；helper 资源为 `install-llamacpp-backend-nsis.cjs` 加最小依赖闭包。当前阶段未执行完整 `dist:win:lite` / `dist:win:full` 安装包构建，端到端安装验证仍留到阶段 7。

### 阶段 6：本地推理页入口收口

目标：删除本地推理页中与 llama.cpp backend 运行时管理相关的用户入口，包括状态展示、启动/停止服务能力、重新安装 backend、切换 backend 等能力。安装阶段已经由 NSIS helper 负责完成 backend 安装，当前阶段不再保留这些页面入口，也不再把它们作为高级维护能力暴露给用户。

交付物：本地推理页 UI 收口清单、被移除入口说明、相关 renderer 测试或最小编译验证结果。

审核点：确认最终用户在 App 内不会再看到 llama.cpp backend 状态展示、启动/停止、重新安装或切换 backend 的入口。

暂不处理项：本阶段不设计也不调整 App 启动时 llama.cpp server 的调用方式；安装完成后如何在后续业务流程中启动/调用 cpp 服务，作为下一步独立工作处理。

阶段 6 执行结果：已完成本地推理页入口收口。`src/renderer/components/localInference/LocalInferenceView.tsx` 不再在页面操作中调用 `llamacpp.install()`、`llamacpp.start()`、`llamacpp.loadModel()`、`llamacpp.unloadModel()`，页面顶部也不再展示 llama.cpp 服务运行/停止状态。`ModelsPanel` 已移除模型卡片中的已加载状态、运行上下文、显存占用、加载模型、卸载模型和卸载中状态入口，仅保留模型下载、取消下载、删除模型、进入推理页等模型管理能力。`InferencePanel` 已移除“服务未运行，请先启动服务”的状态分支，不再把启动服务作为用户可见操作；没有可用已加载模型时仅提示进入模型页。

本阶段没有删除 main/preload 中既有 llama.cpp IPC，也没有调整 App 启动时 cpp server 的启动或调用方式；这些能力仅不再通过本地推理页暴露给最终用户。backend 的重新安装、切换、安装状态等入口在本地推理页面目录中已无引用，后续 backend 安装仍以 NSIS 阶段为准。

验证结果：`rg` 检查确认 `src/renderer/components/localInference` 下已无 `llamacpp.install/start/stop/loadModel/unloadModel` 页面调用，也无 `localInferenceStatus_*`、`localInferenceServiceStopped`、backend switch/update/manager 等本地推理 UI 引用；`npx vitest run src/renderer/components/localInference/LocalInferenceView.marketplace.test.ts src/renderer/components/localInference/inferenceOptions.test.ts` 通过，共 16 个测试；`npm run build` 已通过。普通沙箱内执行 `npm run build` 时 Vite/esbuild 子进程仍会触发 Windows `spawn EPERM`，提权重跑后通过。

### 阶段 7：端到端打包与安装验证

目标：分别构建并安装 `win-lite` 与 `win-full`。验证 `win-full` 离线安装成功；验证 `win-lite` 在线安装成功、断点续传可复用、网络失败时安装中止并提示。安装完成后确认 `%APPDATA%\RongxinAI\llamacpp-runtime\current` 指向正确 backend，App 能启动 llama.cpp 服务。

交付物：两种安装包验证报告、安装日志、App 首启检测结果、失败场景截图/日志。

审核点：确认方案满足发布要求后，再进入清理、文案优化和回归测试。

阶段 7 当前进度（2026-07-04 暂停记录）：已开始 `win-lite` 端到端验证，但尚未完成完整安装包生成与真实安装器验证。首次执行 `npm run dist:win:lite` 时，前置的 `build`、`build:skills`、`prepare:modelscope`、`beforePack` 与 `win-resources.tar` 生成均已通过，`release/win-unpacked` 已生成；打包流程随后在 electron-builder 下载并解压 `winCodeSign-2.6.0.7z` 时失败，错误为无法创建 `darwin/10.12/lib/libcrypto.dylib` 与 `libssl.dylib` 的符号链接，提示“客户端没有所需的特权”。该问题发生在 NSIS 产物生成之前，当前判断为 Windows 当前账号/系统策略缺少创建符号链接权限，导致 winCodeSign 缓存解压失败。

已检查 `win-lite` 打包资源内容：`release/win-unpacked/resources/win-resources.tar` 已包含 `llamacpp-backends/manifest.json` 与 `llamacpp-nsis-helper/install-llamacpp-backend-nsis.cjs`，并包含 helper 所需的 `node_modules` 依赖闭包，例如 `node-downloader-helper`、`extract-zip`、`yauzl`、`fd-slicer`、`pump` 等；资源包中未携带 backend zip，符合 `win-lite` 只携带 manifest、运行安装阶段在线下载推荐 backend 的设计。

为绕过本机 winCodeSign 解压问题，已尝试使用 `npx electron-builder --config.win.signAndEditExecutable=false` 并设置 `RONGXINAI_WIN_LLAMACPP_BACKEND_BUNDLE=lite` 继续生成安装包。该方式成功跳过 winCodeSign 路径，但随后在下载 electron-builder 的 NSIS 二进制缓存 `nsis-3.0.4.1.7z` 时因 GitHub 连接超时失败；重试后仍失败。本机未找到可用的 `makensis.exe`，`%LOCALAPPDATA%/electron-builder/Cache/nsis` 下也只有空缓存目录，因此当前无法生成最终的 `RongxinAI-Setup-2026.6.10.exe` 安装器。

在缺少最终 NSIS 安装器的情况下，已用当前生成的 `release/win-unpacked/RongxinAI.exe` 以 `ELECTRON_RUN_AS_NODE=1` 模拟 NSIS 调用 helper，验证 `win-lite` 安装阶段核心链路。模拟过程从 `win-resources.tar` 解出 helper 与 manifest，传入临时 `--app-data-dir` 与 `--log-path`，选择当前机器推荐的 `b9244/win-x64` backend，并从 `https://rongxinai.krli.org/llamacpp/b9244/llama-b9244-bin-win-cpu-x64.zip` 在线下载归档。日志显示下载、解压、安装与 `current` junction 切换均成功，最终 `tmp/stage7-lite-appdata-20260704-190835/llamacpp-runtime/current/build/bin/llama-server.exe` 存在，`runtime-build-info.json` 写入成功，source 为 `nsis-win-lite`。

阶段 7 尚未完成项：尚未生成真实 `win-lite` NSIS 安装器并运行完整安装流程；尚未执行 `win-full` 打包、资源检查、离线安装模拟与真实安装器验证；尚未验证安装器层面的网络失败弹窗与中止行为；也尚未继续处理 App 首启后如何调用 cpp 服务，因为该事项已按阶段 6 约定留作下一步独立工作。恢复工作时建议先解决本机打包环境问题：为 Windows 启用开发者模式或授予创建符号链接权限以便解压 winCodeSign，或仅在本地验证时继续使用 `--config.win.signAndEditExecutable=false`；同时预置/修复 electron-builder 的 NSIS 二进制缓存，或配置本机可用的 NSIS 工具链。环境问题解决后，再继续 `win-full` 与真实安装器验证。
