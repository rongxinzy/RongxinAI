# Issue #141 / #225 PR 栈评审指南

日期：2026-08-01。本文汇总两个 issue 的全部开放 PR、合并顺序、验证状态与剩余工作，供评审使用。

## 合并顺序

### 链 A：#141 性能主链（必须按序）

1. **#263** `perf/issue-141-phase01` — Phase 0/1：生产压缩、默认无 sourcemap、主功能区 React.lazy、初始化并行化
2. **#271** `perf/issue-141-prefetch` — 导航 hover/focus 预取 lazy chunk（base #263）
3. **#264** `perf/issue-141-phase1-artifacts` — artifact 渲染器按类型 lazy（base #263，与 #271 任意先后）
4. **#266** `perf/issue-141-phase2b-turn-memo` — 增量 turn 稳定化 + TurnBlock memo（base #264）
5. **#268** `perf/issue-141-phase3a-content-visibility` — 离屏 turn 渲染跳过（base #266；**会被 #274 取代，可直接跳到 #274**）
6. **#270** `perf/issue-141-bundle-budget` — CI 构建预算门禁（base #268）
7. **#272** `perf/issue-141-phase3-toolresult` — 超长 tool result 按需 mount（base #268）
8. **#273** `perf/issue-141-lazy-highlight` — Shiki/KaTeX/Mermaid 按需加载（base #272）
9. **#274** `perf/issue-141-phase3b-virtualization` — 会话 turn 全量虚拟化（base #273）
10. **#275** `test/issue-141-turn-fixtures` — 20/200/1000-turn fixture 测试（base #274）

### 链 B：独立 PR（base main，任意顺序）

- **#265** — #141 Phase 2a：流式 delta 整批单次 dispatch、summary 不随 token 更新
- **#267** — #225：Pi 原生运行时类型，与 CoworkRuntime 解耦
- **#269** — #225：OpenClawRuntimeAdapter → OpenClawChannelGateway 重命名
- **#276** — #225 步骤 3：channel:run:event 事件接缝（main 侧）
- **#277** — #225 步骤 3：活动投影数据层（base #276）

注意：#269 与 #267 都改 `agentEngine/index.ts` 的不同行，可同时合并；链 B 与链 A 无文件重叠。

## 关键指标（main 基线 → 链 A 合并后）

| 指标 | 基线 | 现状 | 变化 |
|---|---:|---:|---:|
| 启动图 JS | 9.86 MiB raw / 2243 KiB gz | 2.08 MiB raw / 681 KiB gz | -79% / -70% |
| App chunk | 5093 KiB | ~230 KiB | -95% |
| sourcemap 文件 | 892 | 0 | -100% |
| 200-turn DOM turn 容器 | 全量 | 可视窗口 + overscan（~10-20） | 达标 |

## 验证状态

- 每个 PR：CI（lint/test）绿；本地 `npm test`、双 tsconfig、生产构建通过。
- 11 分支集成树（`integration/issue-141-225`）验证：1667 测试全过、预算门禁绿。
- **未做**（需真机）：production Electron trace、React Profiler before/after、虚拟化交互验证（滚动/rail/导出/分页/流式贴底）。#274 合并前请务必人工过一遍。

## 剩余工作

- #225 步骤 3 的活动流 UI（需要产品设计决策；数据层已在 #277 就绪）。
- #273 合并后：预算脚本 startup 阈值 4.0 → 2.5 MiB。
- 真机 trace/Profiler 补录（#141 验收要求）。
